image: node

pages:
  stage: deploy
  script:
  - mkdir public
  - npx tailwindcss -o ./public/tailwind.css --minify
  - mv ./index.html ./public/
  artifacts:
    paths:
    - public
  only:
  - master
